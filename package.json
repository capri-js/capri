{
  "name": "@capri-js/monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc --build packages --watch",
    "clean": "tsc --build packages --clean",
    "build": "tsc --build packages",
    "examples": "node .github/build-examples.cjs",
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint --fix . && prettier --write .",
    "test": "vitest --run",
    "e2e": "npm run examples && playwright test",
    "release": "multi-semantic-release --sequential-init --deps.prefix ^"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@qiwi/multi-semantic-release": "^7.1.1",
    "@semantic-release/commit-analyzer": "^13.0.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/npm": "^12.0.1",
    "@types/node": "^20.14.9",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.14.1",
    "eslint": "^8.57.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-simple-import-sort": "^12.1.0",
    "prettier": "^3.3.2",
    "typescript": "^5.5.2",
    "vitest": "^1.6.0"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "release": {
    "branches": [
      "main",
      {
        "name": "next",
        "prerelease": true
      }
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/npm",
      "@semantic-release/git"
    ]
  }
}
