{
  "name": "my-capri-preact-site",
  "private": true,
  "description": "Capri Preact example",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build && vite build --ssr",
    "build:github": "BASE_URL=/capri/preact/ npm run build && cp -r dist ../../.github/pages/preact",
    "preview": "vite preview"
  },
  "dependencies": {
    "@capri-js/preact-router": "^4.0.2",
    "preact": "^10.7.2"
  },
  "devDependencies": {
    "@capri-js/preact": "*",
    "@preact/preset-vite": "^2.2.0",
    "vite": "^2.9.9"
  }
}
